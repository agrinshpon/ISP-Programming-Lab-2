myfile = ABOpenfile("newtestfile.txt", True)

myfile.writeat("SESE", 0)
myfile.readat(None, 6)
assert(myfile.readat(None, 0) == "SE")
myfile.close()

myfile = ABopenfile("newtestfile.txt", False)
assert(myfile.readat(None, 0) == "SESE")
myfile.close()